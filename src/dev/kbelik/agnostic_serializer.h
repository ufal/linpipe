#pragma once

#include "common.h"

using namespace linpipe {
using namespace kbelik {
  namespace AgnosticSerializer {
    void build(path wikidata, path kbelik_dump);
    namespace {
      // Functions needed for build.
    }
  };
}
}
